## Change this with the production url or the staging url for SEO purpose
NEXT_PUBLIC_SITE_URL=""

## Backend URL
NEXT_PUBLIC_STRAPI_URL=""

# Sentry
NEXT_PUBLIC_SENTRY_DSN=""

# Google Analytics
NEXT_PUBLIC_GOOGLE_TAG_MANAGER_ID=""

## API Information
API_URL=""
APP_API_KEY=""

# Revalidate Home Route
REVALIDATE_TOKEN=""

# If Next.js logs a warning "API resolved without sending a response", it's a false positive, which may happen when you use `withSentry` manually to wrap your routes. To suppress this warning, set `SENTRY_IGNORE_API_RESOLUTION_ERROR` to 1 in your env.
SENTRY_IGNORE_API_RESOLUTION_ERROR=1
